#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: the first integar
 * @b: the second integar
 * Return: the additional result
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
